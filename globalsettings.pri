# Global settings for qtMobileHotspot compilation
# To be included by application & plugins PRO files

# Switch between release and debug modes
# We must -= the mode to disable, and += the mode to enable
CONFIG += release
CONFIG -= debug

